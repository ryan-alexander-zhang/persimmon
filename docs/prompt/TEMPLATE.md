---
id: prompt-00001-example-slug
type: prompt
status: draft|active|archived
---

# Prompt: <what this prompt drives>

> One sentence: the goal an agent reaches by running this prompt.

## 1. Goal

<What must be true when the run ends. Written as a check, not an intention.>

## 2. Role and Context

<The role the agent assumes and the context it must load first — the files,
docs, or conventions it is held to.>

## 3. Inputs and Output

- Inputs: <what the caller supplies, and what the agent must refuse without>
- Output: <the artefact and its exact shape — path, format, sections>

## 4. Sub-agent Orchestration (optional)

<How work fans out and comes back: which sub-agents run, in what order, and
what each returns. Delete this section when the prompt does not fan out.>

## 5. Prompt

<The reusable prompt body, self-contained, ready to paste as-is.>
