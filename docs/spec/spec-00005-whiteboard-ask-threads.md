---
id: spec-00005-whiteboard-ask-threads
type: spec
status: active
parent: prd-00001-docs-whiteboard
---

# Spec: 答疑线程——问完即走的只读问答，终端答疑退役

> 在节点浮窗或编辑器提问，agent 以 headless 调用在后台作答；每个问题
> 是一个独立会话（答疑线程），追问接续该问的会话；一个文档的线程积累
> 成问题列表，作为编辑器的第三视图态（与编辑、预览并列）可浏览。答疑
> 只读、不占文档、不产生 commit；回答送达走既有桌面通知。终端形态的
> 答疑（`spec-00001-FR-47`）退役。路线与全部取舍见 decision-00012，
> 全部裁决经域主逐条确认。

## 1. Context

- canonical terms 见 `CONTEXT.md`：白板、节点、答疑、Agent 会话、
  会话面板、等待输入、终止、离场、桌面通知、会话历史、会话前快照、
  呈现状态。
- 输入：`parent` 为 [prd-00001-docs-whiteboard](../prd/prd-00001-docs-whiteboard.md)；
  全部取舍在案于 [decision-00012-whiteboard-ask-threads](../decision/decision-00012-whiteboard-ask-threads.md)
  （与本 spec 同轮接收——plan 开启前其必须已 `active`）。
- 本 spec 是 `spec-00001` … `spec-00004` 的**并列新 spec**（Sizing and
  Splitting 第 1 条），同 `parent`，不 supersede 任何一份。
- **与既有文档的交接**（headless 答疑是注册表的第二形态，且首次允许
  同一文档并行多会话；本清单是交接的完整口径，不就地改各文档——
  改写属各自的修订轮，plan-00021 T2 登记）：
  - `prd-00001` §答疑描述（「按对话结论修订」）改写为只读问答——
    prd 修订轮。
  - `spec-00001-FR-47`（终端答疑）**退役**：浮窗的答疑入口保留但
    形态由本 spec 接管（编辑器内的提问入口是**新增** UI）；FR-47 在
    `spec-00001` 修订轮改写为向本 spec 的交接，`spec-00001-AC-47.1`
    … `AC-47.5` 随之改写或移交（不限类型与状态、上下文口径、异常
    拒绝由本 spec 的 FR-1/FR-7 接住）；其「按对话结论修订 `docs/`」
    能力随之移除（域主知情认可），修订走编辑器手改、推进或澄清；
    `spec-00001` Story S9 的价值句同轮改写。
  - `spec-00001-FR-14`：「答疑一会话一 commit」半句移除，
    `spec-00001-AC-14.7`（验证答疑 commit 的 AC）退役——声明保留为
    历史 record 的归属锚点（record 是证据不改写，条目 id 须保持可
    解析），其测试移除；答疑无 commit（FR-4）。
  - `spec-00001-FR-18` 与 `spec-00001-AC-18.1`（并发拒绝的 Unwanted
    面）：对答疑 kind 排除——本 spec 的 `AC-6.1`/`AC-6.2` 断言其
    反面。
  - `spec-00001-FR-12`（终端通道与尺寸）不适用——无 PTY 无终端。
  - `spec-00001-FR-13`（写权限）由 FR-4 收窄为只读。
  - `spec-00001-FR-31`（右侧槽位、编辑器优先）**原样适用**：问题
    列表是编辑器的视图态而非第三占位者（FR-9）。
  - `spec-00001-FR-49`（终止）适用；终止入口的位置对答疑扩展到
    会话面板逐项（FR-7——答疑无终端面板可依，`spec-00003-FR-5`
    的入口位置半句在其修订轮扩展）。
  - `spec-00001-FR-54`（会话历史）适用，转写口径由 FR-5 给定（元
    数据加捕获的回答文本）。
  - `spec-00001-FR-55`（agent 选择）适用，可选集由 FR-2 收窄；
    `spec-00001-AC-55.1` 以答疑为例、断言终端启动——其修订轮改写
    为 headless 首调或换例。
  - `spec-00003-FR-1`（准入正则）：「目标文档没有运行中会话」前件
    与「逐会话终端通道」半句对答疑 kind 排除——不排除则 FR-2 的
    排除（下条）自相矛盾。
  - `spec-00003-FR-2`（文档占用）对答疑 kind **排除**（域主裁定：
    读操作不锁）——FR-6 承载。
  - `spec-00003-FR-4`（会话面板）字段与列序照常；其「点击在终端
    面板呈现该会话」半句对答疑 kind 改为 FR-9 的导航口径。
  - `spec-00003-FR-5`（终端呈现与终止可达）：终止入口位置扩展见
    上；「发起第一个会话时终端面板自动呈现」（`spec-00003-AC-5.4`）
    对答疑不触发；终端面板的任何自动或回落呈现（实现现取最近启动的
    会话）一律不取答疑会话——由 FR-3 作为本 spec 自己的条款承载；
    滚动位置半句不适用。
  - `spec-00003-FR-6`（等待判定）两通路对答疑 kind 排除——FR-6
    承载（无可等的交互输入，静默是思考不是等待）；`spec-00004-FR-2`
    （等待通知）随之对答疑派生不适用。
  - `spec-00003-FR-7`（结束提示条）适用；其「逐会话收尾——一会话
    一 commit 照常执行」半句对答疑不适用（FR-4，无 commit 收尾）。
  - `spec-00003-FR-8`（会话前快照与残余归属）不适用——FR-4 明文
    排除。
  - `spec-00003-FR-9`（正常关停的终止收尾）适用：关停时运行中的
    答疑调用照常走终止收尾，线程记终止态（FR-5）。
  - `spec-00003-FR-10`（节点会话标记）：标记本身适用；其「激活时在
    终端面板呈现该会话」半句由 FR-9 改写——同文档多会话并行后
    「该会话」不再唯一。
  - `spec-00004-FR-3`（结束通知）适用；`spec-00004-FR-5`（点击呈现
    终端面板）对答疑 kind 改为 FR-9 的导航口径——其修订轮登记。
  - `spec-00004-FR-6`（通知内容）不修订：终端答疑退役后 kind 即
    形态；同文档多线程的通知同题不可分辨，为已知边界（§6）。
  - `rule-00001-BR-21`：答疑定义改写为只读问答（修订能力移除），
    `rule-00001-AC-21.3`（验证「结论体现在文档正文」）退役——声明
    保留为历史 record 的归属锚点，其测试移除——rule 修订轮。
- 随接收需修订 `CONTEXT.md`（七处）：「答疑」改写为 headless 只读
  形态，其 _Avoid_ 去掉「问答」（本 spec 以问答为普通名词使用，不再
  是待避词）；「Agent 会话」的「在内嵌终端中实时交互」收窄为终端
  形态所有；「会话面板」的「切换终端呈现的入口」容纳答疑的列表导航
  与逐项终止入口；「桌面通知」的点击行为容纳答疑分支；「终止」的
  按种类 commit 对答疑不适用；「会话前快照」的「四种皆然」收窄为
  终端形态三种（答疑不作快照）；「呈现状态」枚举增列编辑器的问题
  列表视图态与定位项。
- 两份 design 的 `informs` 回链边已随 plan-00021 T1 的修订轮建立
  （design-00001 §10、design-00002 §14）。
- 本 spec 新增术语（拟，接收后进 `CONTEXT.md`）：
  - **答疑线程（Ask Thread）**：一个问题及其追问链——每个新问题开
    一个独立的 agent 会话，追问接续该会话；只读，落盘板外。
    _Avoid_：聊天、评论、对话（宽泛）
  - **问题列表（Ask List）**：一个文档积累的全部答疑线程，作为其
    编辑器的第三视图态（与编辑、预览并列）逐项呈问题与状态、点开
    呈现该项问答，可浏览。
    _Avoid_：问答历史（会话历史是另一词条）、消息列表
  - **headless 调用（Headless Call）**：agent CLI 的非交互一次性
    调用：给定问题、打印回答、进程退出；无 PTY、无终端，靠会话接续
    标识使追问共享同一上下文。
    _Avoid_：API 调用（仍是本地 CLI）、后台会话（会话是注册表的词）

## 2. Stories

| Story | Value | Delivers |
| --- | --- | --- |
| S1 | 作为文档负责人，我读文档时想到一个问题，要在节点或编辑器旁点 Ask、敲下问题就收起继续干活，回答好了再看，这样提问不夺走我的屏幕 | spec-00005-FR-1, spec-00005-FR-3 |
| S2 | 作为文档负责人，我要一个节点的问题积累成可浏览的列表，点开看答案、在该问内追问且 agent 记得此前问答、重启不丢，这样问答是资产不是烟雾 | spec-00005-FR-2, spec-00005-FR-5, spec-00005-FR-9 |
| S3 | 作为文档负责人，我的问题不该挡住这个文档的推进，也不该被推进挡住——读操作不锁，多个问题可以同时问 | spec-00005-FR-6 |
| S4 | 作为文档负责人，我要答疑的调用只读、失败可重发、可终止、配置显式声明，这样轻量问答不带来写盘风险与新机制 | spec-00005-FR-4, spec-00005-FR-7, spec-00005-FR-8 |

## 3. Business Rules

| Rule set | Doc | Covers |
| --- | --- | --- |
| Docs 工作流 | [rule-00001-docs-workflow](../rule/rule-00001-docs-workflow.md) | `rule-00001-BR-21`（答疑非评审动作、不改 status）照旧；其定义中「按对话结论修订文档」随终端答疑退役移除，`rule-00001-AC-21.3` 一并移除——均在 `rule-00001` 修订轮登记（§1 交接）。本 spec 不新增业务规则 |

## 4. System Requirements

- **spec-00005-FR-1** (Event) 当用户对任意类型、任意状态的正常文档
  节点执行答疑时——入口有二：节点浮窗（保留原入口、形态改变）与该
  文档编辑器内的提问入口（新增）；第二十三轮起另有第三来源：标注
  统一提交的 question 通路（`spec-00007-FR-6`——触发主体是提交动作
  而非用户对节点的直接动作，不经此处的问题输入）——系统应提供问题
  输入（不再出现终端），并以问题文本发起一条新答疑线程的首调：指令
  含目标文档路径
  与其全部关系文档路径（沿用原 `spec-00001-FR-47` 的上下文口径）、
  只读答疑的性质说明（回答问题，不修改任何文件），问题文本作为第一
  问附于其后。标注来源的首调指令在上述既有口径之后附该条的选区原文
  及上下文，标注文本作为问题附于其后（第二十三轮增，
  `spec-00007-FR-6`；其 GWT 由 `spec-00007-AC-6.1` 承载，本条不另立
  AC）。
- **spec-00005-FR-2** (Ubiquitous) 每个新问题应开一条独立的答疑线程
  （独立 agent 会话），成为该文档问题列表的一项；线程记录问题、
  回答、提交与回答时刻、所用 agent；追问以线程持有的接续标识接续
  **该线程**的会话，指令只含追问文本（FR-1 的路径与性质说明不重发，
  上下文不重付）；接续只对原 CLI 有效，追问因此沿用该线程的 agent；
  新线程的 agent 选择沿用 `spec-00001-FR-55` 口径，可选集收窄为已
  声明 headless 形态的 agent。
- **spec-00005-FR-3** (Complex) 当一次调用提交后，系统应允许输入
  界面立即收起，且不呈现终端面板——答疑会话永不被终端面板呈现，
  也不作为终端无呈现时的回落对象（`spec-00003-AC-5.4` 的首会话自动
  呈现对答疑不触发；回落不取答疑是本条自己的条款）。进行中状态在
  问题列表项上呈现，节点标记
  与激活行为见 FR-9；页面刷新后按注册表列表恢复（`spec-00003-FR-4`）。
  当调用以零退出结束且捕获到输出时（已回答的唯一判定），该线程呈
  已回答，回答经控制序列剥离后按 Markdown 渲染（复用既有预览渲染）；
  调用结束照常出结束提示条（`spec-00003-FR-7`，其 commit 收尾半句
  对答疑不适用）。线程只增不删（清空入口见 §6）。
- **spec-00005-FR-4** (Ubiquitous) 答疑的 headless 调用应以所选 CLI
  的只读约束形态发起：只读旗标集是 headless 声明（FR-8）的一部分
  （逐 CLI 而异，claude 的缺省声明随 design-00001 修订轮给出）；
  越界写入由 CLI 的权限机制拒绝，白板不做 git 回滚兜底（同
  `spec-00001-FR-13` 的既有口径）。该形态不作会话前快照、完全排除
  于 commit 归属通路之外（`spec-00003-FR-8` 不适用）：调用结束不
  产生任何 commit，仓库残余归写它的会话。答疑不改变文档 status
  （per `rule-00001-BR-21`）。
- **spec-00005-FR-5** (Complex) 问题列表应按文档 id 键控、持久于板
  外存储（与会话转写同侧，不入 git、不进 `docs/`；位置与文件形态属
  design）；文档删除或改 id 后列表不再呈现、存储保留（§6）。服务
  重启后列表与各项问答可见；追问接续原会话为尽力而为——接续标识的
  跨重启有效性由该 CLI 决定，被拒绝的接续按 FR-7 的失败分支处置。
  正常关停时运行中的调用走既有终止收尾（`spec-00003-FR-9`），线程
  记终止态、可重发；服务异常终止遗留的进行中问题，启动时核销为失败
  态、可重发。每次调用的会话历史条目照写（`spec-00001-FR-54`）：
  元数据加捕获的回答文本充当转写。
- **spec-00005-FR-6** (Complex) 答疑调用应计入会话注册表（kind 为
  答疑）：总并发上限适用（`spec-00003-FR-3`），会话面板照常列出
  （`spec-00003-FR-4`，点击导航见 FR-9）；但**不占目标文档**——
  `spec-00003-FR-2` 的文档占用及 `spec-00003-FR-1`/
  `spec-00001-FR-18` 的对应前件与拒绝面对答疑 kind 排除：同文档的
  答疑与推进/澄清/审计互不阻塞，同文档多条线程可并行作答（各自独立
  会话）。答疑 kind 应排除于等待判定的两条通路之外
  （`spec-00003-FR-6` 交接，`spec-00004-FR-2` 随之派生不适用）：
  静默不置位等待输入、不出徽标、不出等待通知。调用结束经既有通路
  通知（`spec-00004-FR-3`，离场时送达），通知内容口径不变
  （`spec-00004-FR-6`；同文档多线程的通知同题不可分辨——已知边界，
  §6）。
- **spec-00005-FR-7** (Unwanted) 若一条线程已有进行中的调用（首调或
  追问），该线程的再次提交应被拒绝且不发起调用（追问天然串行）；
  异常节点不提供答疑入口——浮窗无该入口、其编辑器不呈现提问入口、
  经接口直接请求被拒绝（沿用原 `spec-00001-FR-47` 口径）；无任何
  agent 声明 headless 形态时两处入口均不呈现，经接口请求被拒绝；
  调用以非零退出结束、或 CLI 拒绝接续标识时，该问记为失败态、列表
  既有问答不受影响，问题可重发（重发开新调用）；运行中的调用经会话
  面板**逐项终止入口**（新增——答疑无终端面板可依，终端形态会话的
  终止入口位置不变）可终止（`spec-00001-FR-49`）：进程结束、该问记
  终止态、可重发；系统不以无输出时长判定失败（无超时——手动终止
  兜底）；经接口对答疑会话请求终端接入、输入或尺寸调整应被拒绝。
- **spec-00005-FR-8** (Ubiquitous) agent 条目应可携带
  可选的 headless 声明：首调与接续两种命令形态、问题占位与只读旗标
  （接续标识的捕获口径属 design）；声明不合式时，项目层的条目按
  `spec-00001-FR-15` 启动校验拒绝并点名该条目，本地层的条目按
  `spec-00009-FR-4` 处置（本地层整体忽略、不拦启动）；未声明的 agent
  不进入答疑的可选集（FR-2），全部未声明的拒绝面由 FR-7 承接。
  （第二十六轮修订轮：原「流程配置的 agent 条目」——本地 agent 设置的
  条目同样可声明 headless，`spec-00009-FR-3`。）
- **spec-00005-FR-9** (Complex) 问题列表应作为文档编辑器的第三视图
  态呈现（与编辑、预览并列），槽位规则不变（`spec-00001-FR-31` 原样
  适用——列表随编辑器占用右侧槽位）：逐项呈问题与状态，点开一项
  呈现其全部问答并可追问。会话面板项点击、桌面通知点击与标注列表的
  question 项点击（末项第二十三轮增，`spec-00007-FR-9`；其 GWT 由
  `spec-00007-AC-9.2` 承载，本条不另立 AC）应打开该文档
  的编辑器并切到问题列表态、定位该线程，既有编辑缓冲保留（视图态
  切换不丢缓冲，同编辑/预览切换口径。勘误：初稿把「浮窗答疑入口」
  误列为导航来源——它是提问入口（FR-1），提交即收起、不跳转（FR-3
  与 S1 的「不夺走屏幕」）；无 AC 曾覆盖该误写，域主裁定勘误，
  2026-08-26）；就近处置与
  `spec-00004-FR-5` 同构：会话已不在本次服务运行的列表中时提示且不
  改变当前视图；目标文档不在图上时同样提示且不导航——列表视图寄居
  编辑器，无节点即无编辑器（保守选择，列表存储仍在，FR-5）。节点
  会话标记（`spec-00003-FR-10`）：文档有一个及以上运行中会话即呈现
  一个标记（不逐会话计数）；激活标记时，存在终端形态会话则在终端
  面板呈现它（`spec-00003-FR-2` 对非答疑 kind 仍互斥，同文档至多
  一个终端形态会话），否则切到该文档的问题列表态。

**Acceptance (GWT)**

- **spec-00005-AC-1.1** (spec-00005-FR-1)
  Given 一个 `active` 文档的编辑器已打开
  When 用户经编辑器的提问入口提交问题
  Then 发起一次 headless 首调，指令依次含目标文档路径、全部关系文档
  路径、只读性质说明与该问题文本，不出现终端
- **spec-00005-AC-1.2** (spec-00005-FR-1)
  Given 一个与两份文档互有引用的节点
  When 从其浮窗执行答疑并提交问题
  Then 同一问题输入与首调发起，指令包含那两份关系文档的路径
- **spec-00005-AC-1.3** (spec-00005-FR-1)
  Given 一个 `active` 的 record 节点
  When 执行答疑并提交问题
  Then 首调照常发起——答疑不限类型与状态（承接原
  `spec-00001-AC-47.1`）
- **spec-00005-AC-2.1** (spec-00005-FR-2)
  Given 一条线程已有一问一答
  When 用户在该线程追问
  Then 调用携带接续标识接续同一 agent 会话，指令只含追问文本，线程
  追加第二组问答，各组带提交与回答时刻及所用 agent
- **spec-00005-AC-2.2** (spec-00005-FR-2)
  Given 一个文档已有一条已回答的线程
  When 用户提交一个新问题
  Then 问题列表出现第二条线程，其首调不携带接续标识（独立会话）
- **spec-00005-AC-2.3** (spec-00005-FR-2)
  Given 有效 agent 列表两条，仅其一声明 headless 形态（第二十六轮：原
  「流程配置两条」，理由同 `spec-00001-AC-55.4`）
  When 用户执行答疑
  Then agent 可选集只含声明者；新线程可另选 agent，追问沿用该线程的
  agent
- **spec-00005-AC-3.1** (spec-00005-FR-3)
  Given 一次调用已提交、输入界面已收起
  When 调用以零退出结束且捕获到含控制序列的输出
  Then 该线程呈已回答，回答已剥离控制序列并按 Markdown 渲染，结束
  提示条照常出现且无 commit 收尾
- **spec-00005-AC-3.2** (spec-00005-FR-3)
  Given 一次调用进行中
  When 用户查看该文档的问题列表
  Then 该线程呈进行中
- **spec-00005-AC-3.3** (spec-00005-FR-3)
  Given 一次调用进行中
  When 页面刷新
  Then 进行中状态按注册表列表恢复呈现
- **spec-00005-AC-3.4** (spec-00005-FR-3)
  Given 无任何会话且终端面板未呈现
  When 提交一个问题
  Then 终端面板不呈现（对比 `spec-00003-AC-5.4` 的首会话自动呈现）
- **spec-00005-AC-3.5** (spec-00005-FR-3)
  Given 一次答疑调用是最近启动的会话、另有一个更早的推进会话运行中
  When 页面重连且终端面板无呈现对象
  Then 终端回落呈现推进会话——答疑永不被终端呈现
- **spec-00005-AC-4.1** (spec-00005-FR-4)
  Given 一个 `draft` 文档的一条线程
  When 一次调用完成
  Then 该文档 status 仍为 `draft`，无任何 commit 产生
- **spec-00005-AC-4.2** (spec-00005-FR-4)
  Given 所选 agent 的 headless 声明含其只读旗标
  When 一次答疑调用发起
  Then 实际执行的命令行含该只读旗标——约束到调用形态，不靠 agent
  自觉
- **spec-00005-AC-4.3** (spec-00005-FR-4)
  Given 另一文档的推进会话已写入 `docs/`
  When 本文档的答疑调用先结束
  Then 不产生任何 commit，残余仍归推进会话按 `spec-00003-FR-8` 处置
- **spec-00005-AC-5.1** (spec-00005-FR-5)
  Given 一个文档的问题列表已有问答
  When 服务重启后打开该列表并在一条线程追问
  Then 既有问答可见，追问携带原接续标识发起
- **spec-00005-AC-5.2** (spec-00005-FR-5)
  Given 问题列表存储所在目录
  When 检查版本库
  Then 列表文件不被 git 跟踪，`docs/` 下无问答内容
- **spec-00005-AC-5.3** (spec-00005-FR-5)
  Given 一次调用进行中时服务异常终止（未走正常关停）
  When 服务重启后打开该文档的问题列表
  Then 该问呈失败态且可重发，不呈永久进行中
- **spec-00005-AC-5.4** (spec-00005-FR-5)
  Given 一次调用进行中
  When 服务正常关停
  Then 该调用走终止收尾，重启后该问呈终止态且可重发
- **spec-00005-AC-5.5** (spec-00005-FR-5)
  Given 一次调用以零退出结束
  When 查看会话历史
  Then 该调用有历史条目，含元数据与捕获的回答文本
- **spec-00005-AC-6.1** (spec-00005-FR-6)
  Given 一个文档的推进会话运行中
  When 对该文档提交答疑
  Then 调用照常发起——不因文档占用被拒（改写原 `spec-00001-AC-18.1`
  对此方向的断言）
- **spec-00005-AC-6.2** (spec-00005-FR-6)
  Given 一个文档的答疑调用进行中
  When 对该文档发起推进
  Then 推进照常启动——答疑不占用文档
- **spec-00005-AC-6.3** (spec-00005-FR-6)
  Given 一个文档已有一条进行中的线程
  When 提交另一个新问题
  Then 第二条线程照常发起，两次调用并行运行（各自独立会话）
- **spec-00005-AC-6.4** (spec-00005-FR-6)
  Given 总并发已达上限（`spec-00003-FR-3`）
  When 提交一次答疑调用
  Then 被拒且列表不追加
- **spec-00005-AC-6.5** (spec-00005-FR-6)
  Given 一次答疑调用连续无输出超过静默阈值且进程存活
  When 查看该会话
  Then 不呈现等待输入——无徽标、无等待通知、面板行无等待态
- **spec-00005-AC-6.6** (spec-00005-FR-6)
  Given 桌面通知开关生效且页面离场（`spec-00004-FR-3` 前提）
  When 一次答疑调用结束
  Then 弹结束通知，内容只含种类、文档 id 与状态
- **spec-00005-AC-7.1** (spec-00005-FR-7)
  Given 一条线程已有进行中的调用
  When 在该线程再次提交（追问或重发）
  Then 被拒绝且不发起第二次调用
- **spec-00005-AC-7.2** (spec-00005-FR-7)
  Given 一个异常节点对应的文档
  When 经接口请求答疑
  Then 被拒绝且不发起调用
- **spec-00005-AC-7.3** (spec-00005-FR-7)
  Given 一个异常文档（其编辑器仍可达，`spec-00001-FR-2`）
  When 打开其编辑器
  Then 编辑器不呈现提问入口
- **spec-00005-AC-7.4** (spec-00005-FR-7)
  Given 全部 agent 均未声明 headless 形态
  When 打开正常节点的浮窗与编辑器
  Then 均不呈现答疑入口；经接口请求被拒绝
- **spec-00005-AC-7.5** (spec-00005-FR-7)
  Given 一次调用以非零退出结束
  When 查看该线程并重发该问
  Then 该问此前呈失败态、列表既有问答完好，重发发起新调用
- **spec-00005-AC-7.6** (spec-00005-FR-7)
  Given 一次答疑调用进行中
  When 用户经会话面板该项的终止入口终止它
  Then 进程结束，该问呈终止态且可重发
- **spec-00005-AC-7.7** (spec-00005-FR-7)
  Given 一次答疑调用进行中
  When 经接口对其请求终端接入、输入或尺寸调整
  Then 三者均被拒绝
- **spec-00005-AC-8.1** (spec-00005-FR-8)
  Given 项目层一条 agent 条目的 headless 声明缺少必需占位
  When 服务启动
  Then 启动被拒且报错点名该 agent 条目（`spec-00001-FR-15` 口径）
- **spec-00005-AC-8.2** (spec-00005-FR-8)
  Given 一条 agent 配置携带合式的 headless 声明
  When 一条线程首调与追问相继执行
  Then 两次调用各按声明的首调与接续命令形态执行
- **spec-00005-AC-8.3** (spec-00005-FR-8)
  Given 本地层一条追加条目的 headless 声明缺少必需占位（第二十六轮增）
  When 服务启动
  Then 服务照常启动，该条目不在有效 agent 列表中，设置面板点名该条目的
  headless 声明
- **spec-00005-AC-9.1** (spec-00005-FR-9)
  Given 一个文档有三条线程：已回答、进行中、失败
  When 打开其编辑器并切到问题列表态
  Then 逐项呈问题与对应状态，点开已回答项可见其全部问答并可追问
- **spec-00005-AC-9.2** (spec-00005-FR-9)
  Given 该文档编辑器打开在编辑态且缓冲有未保存修改
  When 用户点击该文档一条答疑调用的桌面通知
  Then 编辑器切到问题列表态并定位该线程，未保存缓冲保留
- **spec-00005-AC-9.3** (spec-00005-FR-9)
  Given 一次答疑调用进行中或已结束
  When 在会话面板点击该项
  Then 打开该文档编辑器的问题列表态并定位该线程
- **spec-00005-AC-9.4** (spec-00005-FR-9)
  Given 一条答疑调用的结束通知已弹出、其目标文档已不在图上
  When 用户点击该通知
  Then 提示目标不在图上，当前视图不变（列表存储仍在）
- **spec-00005-AC-9.5** (spec-00005-FR-9)
  Given 一条答疑调用的通知已弹出、其会话已随服务重启不在列表中
  When 用户点击该通知
  Then 提示该会话已不在，当前视图不变
- **spec-00005-AC-9.6** (spec-00005-FR-9)
  Given 同一文档一条答疑调用与一个推进会话并行运行
  When 查看该节点并激活其会话标记
  Then 节点只呈现一个标记，激活后终端面板呈现推进会话
- **spec-00005-AC-9.7** (spec-00005-FR-9)
  Given 一个文档仅有答疑调用运行中
  When 激活其节点会话标记
  Then 打开该文档编辑器的问题列表态——不呈现终端

## 5. Technical Design

| Design | Doc | Covers |
| --- | --- | --- |
| 服务端（第二十一轮已扩展，§10；回链已建） | [design-00001-docs-whiteboard](../design/design-00001-docs-whiteboard.md) | headless 执行通路（捕获输出，无 PTY）、问题列表存储位置与文件形态、接续标识捕获口径、claude 缺省 headless 声明（含只读旗标集）、注册表第二形态（不占文档、无等待、拒绝终端接入）、正常关停与启动核销、声明的键名与占位语法、`POST /api/sessions/ask` 的语义改造 |
| 白板 UI（第二十一轮已扩展，§14；回链已建） | [design-00002-whiteboard-ui](../design/design-00002-whiteboard-ui.md) | 浮窗与编辑器双入口、问题输入、编辑器第三视图态（列表、点开、追问、失败重发、缓冲保留）、节点标记的多会话口径与激活分支、面板逐项终止入口、面板与通知点击导航、前端锁与标记推导改造（原按「一文档一会话」建映射） |

## 6. Out of Scope

- 答疑修订文档的任何能力（只读是站立约束，`decision-00012` §5；修订
  走编辑器、推进或澄清）
- 线程与列表的清空/删除入口（只增不删，FR-3；清空留将来轮）；文档
  删除或改 id 后遗留的列表存储（不呈现、保留，FR-5——回收留将来轮）
- 无输出超时判败（FR-7 明文不做——手动终止兜底）
- 同文档多条线程的桌面通知与提示条同题不可分辨（`spec-00004-FR-6`
  内容口径不动，路由靠会话标识——已知边界，宁简勿改）
- 问答内容的搜索、导出、跨文档汇总，及线程长度/回答体量的配额
- 多用户/多浏览器对同一列表的并发追问协调（单人工具的既有前提）
- 跨服务重启的进行中调用存续（`spec-00003-FR-9` 口径；FR-5 承诺的
  是正常关停记终止、异常终止核销失败）
- 推进/澄清/审计三种会话的任何改动（终端通道原样保留）

## 7. Open Questions

无——本轮裁决闭合：形态统一与双入口、无等待只有结束通知、问题附于
既有指令后、按问追问（resume）、只读不锁、问题并行/追问串行，经域主
逐条确认（在案于 `decision-00012`）；两轮审计揭出的四个新状态（多
会话节点标记、无终端的终止入口、列表的槽位归属、关停与崩溃的线程
终态）已在 FR-5/FR-7/FR-9 落成条文；claude 缺省 headless 声明的具体
旗标集与接续标识捕获口径委给 design-00001 修订轮（§5）。

## Links

- Parent: [prd-00001-docs-whiteboard](../prd/prd-00001-docs-whiteboard.md)（答疑描述改写属其修订轮，§1 交接）
- Sibling specs: [spec-00001-docs-whiteboard](spec-00001-docs-whiteboard.md)（FR-47 退役交接、FR-14/FR-18/FR-55 触及条目见 §1，属其修订轮）·
  [spec-00003-whiteboard-parallel-sessions](spec-00003-whiteboard-parallel-sessions.md)（注册表复用；FR-1/FR-2/FR-4/FR-5/FR-6/FR-7/FR-8/FR-10 的交接见 §1，属其修订轮）·
  [spec-00004-whiteboard-desktop-notifications](spec-00004-whiteboard-desktop-notifications.md)（结束通知通路；FR-2/FR-5 交接见 §1，属其修订轮）
- Rules: [rule-00001-docs-workflow](../rule/rule-00001-docs-workflow.md)（BR-21 只读改写与 `rule-00001-AC-21.3` 退役已随 plan-00021 T2 落地）
- Design: [design-00001-docs-whiteboard](../design/design-00001-docs-whiteboard.md) §10 ·
  [design-00002-whiteboard-ui](../design/design-00002-whiteboard-ui.md) §14（均已随 plan-00021 T1 扩展并回链）
- Decisions: [decision-00012-whiteboard-ask-threads](../decision/decision-00012-whiteboard-ask-threads.md)（本 spec 的全部取舍，同轮接收）·
  [decision-00006-whiteboard-ask-clarify](../decision/decision-00006-whiteboard-ask-clarify.md)（答疑与澄清的方向切分不变；答疑的形态由本 spec 接管）
