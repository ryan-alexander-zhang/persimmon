# Templates — Scaffold Router

This is a meta-skill. It does not generate code artifacts.

## Skeleton routing output (what you should produce as the assistant)
- Selected module: `domain|app|infra|adapter|start`
- Selected package root and file paths (consistent with `package-info.java`)
- Selected generator skill name
- Required missing inputs (ask 1 question when needed)
- Guardrails checklist items that apply

