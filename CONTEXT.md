# Docs Whiteboard

docs 工作流可视化操作台的语境：人与 agent 围绕 `docs/` 目录协作时使用的统一语言。

## Language

**白板（Whiteboard）**：
`docs/` 目录的可视化视图与操作台；Markdown 文件是唯一事实来源，白板可随时丢弃重建。
_Avoid_：画布（指白板内的绘图区域时除外）、看板

**节点（Node）**：
白板上代表一个 docs 文档的图元，展示其类型、id、标题与状态。（组节点代表一组文档，不是节点。）
_Avoid_：卡片、块

**关系边（Edge）**：
白板上代表一条 front matter 关系声明的图元，自声明方指向被引用方。折叠态下落在组节点上的汇聚边代表组内文档的若干条声明。
_Avoid_：依赖线、引用线

**弱化态（Dim）**：
未选中任何节点时关系边的默认呈现：可见但不夺目，且不带关系名。

**强调态（Emphasis）**：
与当前选中节点相连的关系边的呈现：高对比、带关系名、绘于节点之上。

**压弱（Suppress）**：
选中某节点时，对与它无关的边与节点所做的进一步淡化。
_Avoid_：隐藏（压弱的东西仍然可见）

**关系列表（Relation List）**：
选中节点的工具栏中按关系字段分组列出的全部关系，每项含字段名、方向与对端文档 id。
_Avoid_：依赖列表、引用列表

**需求条目（Requirement Item）**：
spec 的 FR 与 rule 的 BR 的统称——文档内部带正式编号的最小可验收单位；AC 附属于条目，不是条目。
_Avoid_：需求点、规则条款

**验收行（Acceptance Row）**：
record 验收清单表格中「被验 id · 测试 · 结果」的一行，被验 id 是 AC id 或需求条目 id；覆盖状态的唯一证据来源。
_Avoid_：测试行、清单行

**覆盖状态（Coverage）**：
由验收行推导出的需求条目验证情况，三态：已验证、未通过、未覆盖。
_Avoid_：测试状态、通过率

**全局覆盖率视图（Coverage Overview）**：
顶栏入口打开的全屏对话框，按文档列出每份 spec/rule 的需求条目覆盖三态计数，
可展开单份看逐条目状态；与检视面板同一推导，但看的是全仓而非选中的一份，由
`spec-00002-FR-10` 持有。
_Avoid_：聚合面板、覆盖率报表、总览

**交付范围（Delivery Scope）**：
一个 plan 经 `implements` 声明的、其完成所须验证的需求条目集合：条目 id 按
条目计入，AC id 将其所属条目计入，整份 spec/rule 文档 id 将其全部条目计入，
其他类型目标不计入，由 `rule-00001-BR-24` 持有。
_Avoid_：范围、scope（泛指时）

**resolved 门（Resolved Gate）**：
plan 从 `open` 促为 `resolved` 时的守门判定：以 `parent` 指向该 plan 的
record 为证据，交付范围内每个条目的覆盖状态须为已验证，否则拒绝流转，由
`rule-00001-BR-25` 持有。
_Avoid_：验收门、执法（泛指时）

**检视面板（Inspector Panel）**：
选中 spec/rule 节点后停靠右侧、列出其需求条目与覆盖状态的面板；与编辑器互斥占用右侧槽位。
_Avoid_：属性面板、详情栏

**子画布（Sub-canvas）**：
下钻进单份 spec/rule 后的画布：需求条目、AC 与验收行是其中的节点，面包屑返回顶层。
_Avoid_：详情视图、二级白板

**详情面板（Detail Panel）**：
子画布中单击节点后停靠右侧槽位的只读全文视图；卡片管辨认，详情管阅读。
_Avoid_：属性面板、侧边栏

**条目文法（Item Grammar）**：
spec/rule 的条目声明与 record 验收行的机器可读写作形态，各文件夹 README 的「机器可读形态」小节持有；白板与 agent 产出都受它约束。
_Avoid_：格式规范、模板规则

**刷新（Refresh）**：
白板重取服务端数据（图、当前选中或下钻文档的条目，以及会话状态）并重绘、同时按 id 保住呈现状态的一次动作；三个触发来源——变更推送、白板自身动作、会话结束（有无 `docs/` 变更皆然）——共用同一条通路。「重取」只指其中取数据的那一步。
_Avoid_：手动刷新（指浏览器重载页面时说「重新加载页面」）、reload

**变更推送（Change Push）**：
服务端监听到 `docs/` 变化后向已连接的白板广播的无载荷信号；白板收到即刷新。
_Avoid_：轮询、同步、推送通知（与「桌面通知」也无关——本词条是服务端到
页面的内部信号，桌面通知是页面到用户桌面的呈现）

**呈现状态（Presentation State）**：
只活在界面里、不进 `docs/` 的状态：当前选中、下钻所在文档、检视面板的展开行、详情面板的目标、全局覆盖率视图的展开行、当前呈现的会话与各会话的终端滚动位置、编辑器的问题列表视图态及其定位线程、编辑器的标注列表视图态及其定位标注、导航栏的开合与各类型组的展开态、各目录组的展开态（画布与导航栏共享）、设置面板的开合。刷新后按 id 保持（导航栏的两项与各目录组的展开态还跨页面重载保持）。
_Avoid_：UI 状态（过泛）、上下文

**就近关闭（Close Nearest）**：
刷新后某呈现状态的所指对象已不存在时，只关掉依赖它的那一级，不连锁清空其余各级。
_Avoid_：重置、回退

**会话前快照（Pre-session Snapshot）**：
Agent 会话（终端形态四种皆然，`spec-00003` 前仅推进、共写自 `spec-00006` 起；答疑不作快照，`spec-00005-FR-4`）启动时记录的 `docs/` 已脏路径及其内容摘要；会话结束时以内容差集确定该次 commit 的暂存集。共写的快照对脏路径**另存全文**以支持收束复原（design-00001 §11.3 的扩义）。
_Avoid_：基线、备份

**解析诊断（Parse Diagnostics）**：
不合条目文法的行的显性清单——疑似条目而形态残缺、验收行不合式、无法归属；白板呈现并计数，不使文档节点转异常。
_Avoid_：解析错误（诊断不阻塞任何功能）、警告列表

**异常清单（Anomaly List）**：
点击顶栏异常计数展开的清单，逐条列出每个异常的来源与 problem 文本；边的异常
归属声明方文档，点击一条即定位到对应节点，由 `spec-00002-FR-13` 持有。
_Avoid_：错误列表、问题面板

**诊断清单（Diagnostics List）**：
点击顶栏诊断计数展开的清单，逐条列出每条解析诊断的来源文档 id、类别与原文行；
与异常清单各自独立、内容不混，由 `spec-00002-FR-14` 持有。
_Avoid_：警告列表、解析报告

**锚点（Handle）**：
关系边落在节点上的位置；白板只用它定位既有的关系边，不据此建边。
_Avoid_：接口、连接桩

**类型列（Type Column）**：
白板上属于同一文档类型的那一列；同列即同类型，跨列即跨类型。
_Avoid_：分组、泳道、层

**评审动作（Review Action）**：
文档负责人在节点上做出的把关动作，只有三种：接收、澄清、审计。
_Avoid_：审批、审核操作

**接收（Accept）**：
认可一个 `draft` 文档，将其按文档种类促为 `active`（living doc）或 `open`（work item）。
_Avoid_：通过、批准

**澄清（Clarify）**：
对 `draft` 的可澄清类型文档发起的评审动作：agent 会话带着该文档及其关系文档
逐题向负责人提问，确认的未决点记入 Open Questions、既定结论直接修订文档，
文档保持 `draft`。
_Avoid_：打回、驳回

**审计（Audit）**：
对 `draft` 的可审计类型文档发起的评审动作：agent 会话对照该类型文件夹的
README 先审结构与文法、再审内容本身，未决发现记入该文档的 Open Questions、
既定结论直接修订正文，文档保持 `draft`，由 `rule-00001-BR-22` 持有。
_Avoid_：检查、复核、review

**可审计类型（Auditable Type）**：
允许发起审计的文档类型，恰为 spec、rule、design 三种，由 `rule-00001-BR-23`
持有；白板以代码内建该集合（同可澄清类型集）。

**可澄清类型（Clarifiable Type）**：
允许发起澄清的文档类型，恰为 idea、prd、spec、rule、design 五种，由
`rule-00001-BR-20` 持有；白板以代码内建该集合（同状态流转表），流程配置为
其中每型持有焦点行。

**答疑（Ask）**：
对任意类型、任意状态的正常文档节点（异常节点除外）发起的只读问答：用户
从节点浮窗或编辑器提问，agent 以 headless 调用后台作答；不修改任何文件、
不产生 commit；不是评审动作，不改变文档状态（`spec-00005` 持有；终端
形态已退役）。
_Avoid_：咨询

**答疑线程（Ask Thread）**：
一个问题及其追问链——每个新问题开一条独立的 agent 会话，追问接续该
会话；只读，落盘板外。
_Avoid_：聊天、评论、对话（宽泛）

**问题列表（Ask List）**：
一个文档积累的全部答疑线程，作为其编辑器的一个视图态（与编辑、预览、
标注列表并列）逐项呈问题与状态、点开呈现该项问答，可浏览。
_Avoid_：问答历史（会话历史是另一词条）、消息列表

**headless 调用（Headless Call）**：
agent CLI 的非交互一次性调用：给定问题、打印回答、进程退出；无 PTY、
无终端，靠会话接续标识使追问共享同一上下文。
_Avoid_：API 调用（仍是本地 CLI）、后台会话（会话是注册表的词）

**焦点行（Focus Line）**：
流程配置中每个可澄清类型的一句话，承载该类型的提问重心、负向边界与
停止条件（「本阶段推进决策」的逐类型判据，`spec-00001-FR-48`）；澄清任务
指令的提问重心 = 共享骨架 + 该类型的焦点行。
_Avoid_：类型提示词、提示词模板

**收敛声明（Convergence Call）**：
澄清骨架的一项要求（`spec-00001-FR-45`，第十二轮）：以最少的问题解决本
阶段的推进决策，剩余问题不再改变该决策时声明澄清已饱和并进入收尾，判据
取自焦点行的停止条件。
_Avoid_：收敛判定、停止规则（那是焦点行里的判据，不是骨架的这项要求）

**澄清状态文件（Clarify State File）**：
澄清会话逐题落盘的提问进度 JSON，位于 `docs/` 之外且不入 git；同一文档再次
澄清时据它恢复，全部结论落盘后删除。
_Avoid_：会话缓存、断点文件

**状态流转（Status Transition）**：
文档 `status` 沿合法路径的一次变更；合法路径由 `rule-00001-docs-workflow` 的决策表定义。
_Avoid_：状态迁移、状态变更

**促进（Promote）**：
沿状态流转把 `draft` 文档向前推一步（living doc 促为 `active`，work item 促为 `open`）。
_Avoid_：升级、提升

**促进门（Promotion Gate）**：
文档促进出 `draft` 时的守门判定：带未决 Open Questions 者一律拒绝，接收与状态
切换两条通路同一判定，由 `rule-00001-BR-12` 持有、`spec-00002-FR-1` 承载。
_Avoid_：接收门、OQ 检查

**归档门（Archive Gate）**：
文档转为 `archived` 时的守门判定：仓库中须存在另一份 `supersedes` 列出其 id
的文档，否则拒绝，由 `rule-00001-BR-19` 持有、`spec-00002-FR-3` 承载。
_Avoid_：supersedes 校验、归档检查

**预览（Preview）**：
编辑器当前缓冲区的渲染视图，与编辑互斥切换；渲染的是未落盘的缓冲区正文，不含 front matter。
_Avoid_：渲染视图、实时预览

**动作被拒（Refusal）**：
系统对一次不合法动作的拒绝及其反馈（非法流转、resolved 门拒绝、冲突、文档已不存在等）；反馈以提示条呈现。
_Avoid_：拒绝（该词专指白板不提供的评审动作）、驳回

**命令面板（Command Palette）**：
按 id 或标题检索文档并跳转到该节点的入口，快捷键唤起。
_Avoid_：搜索框、快捷菜单

**导航栏（Navigation Sidebar）**：
工作区左侧常驻、可收起、宽度可调的文档目录：按类型列的列序列出各类型组、组内先列顶层文档、再列目录组，每行含状态、id 与标题；点一行即定位并选中该节点，选中变化时同步高亮，由 `spec-00008` 与 `spec-00010-FR-8` 持有。
_Avoid_：侧边栏（泛指）、文件树、目录树（物理目录不是分列依据）、资源管理器

**类型组（Type Group）**：
导航栏中同一类型列的文档构成的一组，可折叠；组序即列序，组内序即行序；其下可再分目录组（`spec-00010`）。（「类型列」是画布一侧的名字，类型组是导航栏一侧对同一集合的名字。）
_Avoid_：分组、文件夹、目录（指其归组依据时）

**缩略图（MiniMap）**：
画布右下角的整图缩略视图，每个节点一个按状态着色的缩略块、折叠的目录组一块，标出当前视口范围，由 `spec-00008-FR-7` 与 `spec-00010-FR-10` 持有。
_Avoid_：小地图、概览图、导航器

**异常（Anomaly）**：
文档的 front matter 不合法、或关系指向不存在的文档；白板在对应节点或边上标记它。
_Avoid_：问题、错误、待办问题

**撞 id（Duplicate Id）**：
两份或以上文档解析出同一 id 的状态；白板以文件路径为键把它们各自呈现并标记
异常，其条目不参与覆盖推导与 resolved 门，由 `spec-00002-FR-8` 持有。
_Avoid_：id 冲突（指单次动作被拒时说「先修复撞 id」）、重复文档

**共写（Co-write）**：
对一份 `draft` 文档（work item 亦可在 `open`）发起的多轮协作写作会话：
agent 按对话中的意图与材料起草或改写正文，等待输入期间用户可直接手改
（编辑器缓冲在**无未保存改动时**随 agent 写入重载，有则保留并告知；
非等待期只读——只读只作用于 Source 视图的编辑与保存），结束时经收束
过滤一次 commit——仅目标文档与合式新建 `reference` 落地；不是评审动作，
不改变文档状态，由 `rule-00001-BR-28` … `BR-30` 与 `spec-00006` 持有。
_Avoid_：协作编辑、AI 起草、修订会话、代写（那是推进的形态）

**终止（Stop）**：
对运行中 Agent 会话的用户主动结束：立即结束进程，退出收尾（结束态、按会话
种类 commit——答疑无 commit、共写经收束过滤，`spec-00006-FR-6`/`FR-8`——收尾、刷新）照常走一次；不是评审动作，不删除澄清状态文件。服务端
正常关停时对运行中会话的收束视同终止（`decision-00009` §2 第 10 条）。
_Avoid_：取消、中断、kill

**推进（Advance）**：
从一个节点按流程配置发起下一阶段文档的创建（如 idea → prd），由 agent 会话
执行；不设 OQ 门——来源文档带未决 Open Questions 时照常可推进，未决点经
上游未决点继承显式传递。
_Avoid_：流转、派生

**上游未决点继承（Upstream Question Inheritance）**：
推进任务指令的一项要求（`spec-00001-FR-11`，第十三轮）：目标类型带
Open Questions 语义时，读取来源文档的未决 Open Questions，把仍影响新文档
的继承进新文档的 Open Questions 小节，不得沉默替上游做决定。
_Avoid_：OQ 传递、问题下沉

**修订轮（Revision Round）**：
把一份 `active` 的 living doc 经合法状态流转转回 `draft` 以修订它；回到
`draft` 后审计、澄清与接收门按既有语义适用，上一有效版由 git 历史承载，由
`rule-00001-BR-3` 持有。（「促进」专指向前流转，反向的这一步是状态流转，
不是促进。）
_Avoid_：回退、降级、促回

**流程入口类型（Entry Type）**：
可不经推进直接新建、由此进入流程的类型（「入口」取广义：作图的起点，
不限于产品流三段），恰为 idea、prd、design、analysis、decision、
integration、reference、operation、prompt、report（`rule-00001-BR-26`
持有，design/analysis 第十四轮增，后六者 `decision-00014` 增）：idea 与
prd 是产品流的起点，其余八种在产品流中不是任何类型的下一步候选、无推进
通路可走，入口是其唯一出生方式；流程配置以 `entry` 列表为其机器可读
形态；非入口类型的新建被拒（`rule-00001-BR-27`）。
_Avoid_：根类型、起始类型、入口类型（省称——全称含「流程」）

**新建（Create）**：
在白板上直接创建一份流程入口类型的文档：id 自动取号、模板预填、初始
`draft`；空白模式保存即建档并 commit，共写模式确认即建档并 commit、随即
接续共写会话（`spec-00006-FR-2`）；不是推进——它没有源文档。
_Avoid_：创建节点、添加文档

**会话历史（Session History）**：
已结束会话的转写与元数据，落盘于 `.whiteboard/sessions/`、重启不丢，可在
白板中列出与查看；不入 git，与留痕（commit 历史）互补。
_Avoid_：会话日志、聊天记录

**会话面板（Session Panel）**：
顶栏入口打开的面板，列出全部运行中会话与本次服务启动以来的已结束会话，
每项含会话种类、目标文档 id 与状态，是切换终端呈现的入口（答疑项的
点击导航到其问题列表并定位线程），并逐项提供终止入口；入口呈现
「运行中数/上限」与等待计数徽标，由 `spec-00003-FR-4` 持有。
_Avoid_：会话列表（面板还承载切换与徽标）、任务面板

**等待输入（Awaiting Input）**：
运行中会话的呈现子态：连续无输出达到静默阈值且进程存活，或输出流中出现
终端通知转义序列（OSC 777，置位后锁存至用户输入或会话结束），视为停下来
等用户回答；计入运行中总数，只驱动徽标、提示条与桌面通知，不是新的会话
生命周期阶段，由 `spec-00003-FR-6` 持有（弱语义，允许误报）。
_Avoid_：阻塞、挂起、pending

**桌面通知（Desktop Notification）**：
白板页面离场时经浏览器原生通知能力弹出的系统级通知，只含会话种类、
文档 id 与状态，点击回到白板呈现对应会话（答疑呈其问题列表并定位
线程）；显式开关启用，页面可见且聚焦时不弹，标签页关闭后不送达，
由 `spec-00004` 持有（取舍见 `decision-00010`）。
_Avoid_：系统通知、浏览器通知、推送通知（「变更推送」词条排除的词）

**离场（Away）**：
白板页面不在用户眼前的状态——页面隐藏（标签页后台等），或页面所在
窗口失焦（在别的窗口干活）；两个信号的具体判定口径属 design。
_Avoid_：不在线（会话与服务都还在）、后台（只覆盖隐藏半边）

**离场区间（Away Stint）**：
从一次转入离场到下一次回到页面之间的连续区间；等待通知的判重单位——
每会话一个区间内至多一条，由 `spec-00004-FR-2` 持有（issue-00020：
以标志翻转为界会被 CLI 空闲重绘轰炸）。
_Avoid_：离场期间（口语说法，正名为区间）、等待回合（已废弃的旧判重单位）

**流程配置（Flow Config）**：
机器可读的配置文件，定义文档类型、关系字段、每种类型的下一步候选、可澄清类型的焦点行、agent 条目（命令、写权限约束，可带 `model` 与 `env`——它是 agent 设置的项目层）、会话并发上限（`max_sessions`）、配置排除（`exclude`）；白板读它而不解析散文规范。
_Avoid_：流程图、工作流定义

**关系矩阵（Relation Matrix）**：
流程配置中「类型 → 允许的关系字段」的机器可读映射，承载各文件夹 README
「Relations」小节的约定；`parent` 单值一条不在矩阵内、由代码持有，二者共用
同一条解析诊断类别。不配对者进解析诊断而非节点异常，由 `spec-00002-FR-5` 持有。
_Avoid_：字段白名单、关系表

**Agent 会话（Agent Session）**：
白板通过本地 CLI（Claude Code / Codex 等）发起的一次代理过程——推进的文档代写、澄清的逐题提问、审计的对照 README 审查、共写的多轮协作写作（四者在内嵌终端中实时交互），或答疑的 headless 调用（无终端）；写权限按流程配置约束（MVP 默认仅 `docs/`，答疑只读，共写另经收束过滤收窄到目标文档与合式新建 `reference`），所用 agent 取自有效 agent 列表，浏览器断开后会话在服务端存续。
_Avoid_：AI 任务、机器人

**Agent 设置（Agent Settings）**：
agent 从哪来、怎么启动这件事的两层配置（项目层 + 本地层）与其设置面板的统称。
_Avoid_：agent 配置、agent 管理、模型设置（模型只是条目的一个键）

**Agent 条目（Agent Entry）**：
以名字为键的一条 agent 声明：`command`、`args`、`cwd`，可选的 `model`、`env`、`headless`。项目层的条目在流程配置 `agents` 下；本地层的条目是对项目条目的覆盖或本机独有的追加。
_Avoid_：profile、预设

**本地 agent 设置（Local Agent Settings）**：
简称本地层。`.whiteboard/` 下不入 git 的一份文件，按名字对项目层条目做键级覆盖、追加本机独有条目、标记禁用与缺省；由设置面板写，手改亦可。对应地，流程配置的 `agents` 称项目层。
_Avoid_：用户配置、本地配置（泛指）、override 文件

**有效 agent 列表（Effective Agent List）**：
简称有效列表。项目层与本地层合并后的有序列表——一切「可选 agent」「缺省 agent」的唯一来源；每次会话发起受理与配置下发时重新计算。
_Avoid_：合并配置、最终配置、注册表

**设置面板（Settings Panel）**：
顶栏入口打开的面板，呈现有效 agent 列表并编辑本地 agent 设置。
_Avoid_：偏好、选项、配置页

**行内 id 跳转（Inline Id Jump）**：
检视面板、子画布与详情面板的正文中，内容恰为一个可解析 id 的反引号行内代码
的激活跳转：回到顶层白板，定位并选中该 id 所属的文档节点；撞 id 与异常文档
的条目 id 不可解析；只作用于呈现层，不建边、不进解析诊断，由
`spec-00001-FR-57` … `FR-59` 持有。
_Avoid_：id 链接（不产出链接元素）、散文解析（白板依旧不解析散文建图）

**标注（Annotation）**：
编辑器编辑或预览视图态下，选中一段文本后附着其上的提问或改进请求，类型恰为
question（复用答疑，只读）与 issue（转回 `draft` 并发起共写）两种；一份文档
可攒多条，统一提交后逐条可见进展（`spec-00007` 持有）。
_Avoid_：批注、评论、comment（该类型已在 idea 阶段裁掉）

**选区锚（Text Anchor）**：
标注定位到文档的方式：锚定选区文本及其上下文，不锚行号；命中判定以锚整体
为键（上下文参与消歧），定位成功要求恰一处命中；只服务于编辑与预览里的
定位，处理的兜底是标注自身携带的选区原文引用。
_Avoid_：锚点（已属关系边）、行号定位

**孤儿标注（Orphaned Annotation）**：
选区锚定位不到（锚键被改或删，或在文中多处等同命中）的标注。提交时校验
发现即单条拦下，待重新选区或删除，其余照提交；提交后失效不中断处理，退化
为「原文已变更」提示。
_Avoid_：失效标注、死标注

**标注列表（Annotation List）**：
一份文档全部标注的呈现入口，逐条呈类型与进展——question 的回答、issue 的
处理与修改；与问题列表（答疑）是两样东西，并列呈现为编辑器的第四视图态
（design-00002 §16.1 裁定）。
_Avoid_：问题列表（已属答疑）、评论列表

**留痕（Audit Trail）**：
白板发起的每次文件变更都落为一次 git commit，commit 信息指明动作与文档 id。（英文 Audit Trail 与评审动作「审计（Audit）」无关——后者是对文档内容的审查，前者是变更历史。）
_Avoid_：日志、历史记录

**配置排除（Exclude）**：
流程配置 `exclude` 字段列出的、相对 `docs/` 的 glob 模式集；命中的文件对白板不存在——不成节点、不进异常与诊断、不可检索、不占号、其文档 id 与条目 id 不可被解析，由 `spec-00010-FR-1` 持有。与模板文件排除叠加，两者互不替代。
_Avoid_：忽略（ignore，gitignore 的联想会带来「仍占位」的误读）、隐藏、过滤

**模板文件排除（Template-file Exclusion）**：
任何层级下文件名为 `README.md` 或 `TEMPLATE.md` 的文件不作为文档（`spec-00001-AC-1.3`）。
_Avoid_：README 排除、内置排除

**目录组（Directory Group）**：
同一类型列内、`docs/` 下路径前两段相同的文档构成的一组；组键即那两段路径；画布上以一个组节点占位、导航栏中以类型组之下的一层呈现；可折叠，两处共享展开态，由 `spec-00010-FR-4` 持有。
_Avoid_：文件夹、子目录（指物理目录本身时除外）、分组、子组

**组节点（Group Node）**：
折叠态目录组在其类型列内占据的图元：呈现组名与文档数，带异常与会话的聚合标记，点击即展开或折叠；代表一组文档而非一份，不是节点，不可被选中为文档，由 `spec-00010-FR-5` 持有。
_Avoid_：文件夹节点、聚合节点、父节点

**组名（Group Name）**：
组节点与导航栏组头呈现的名字，取自组键：组键第一段与该列类型同名时只呈现第二段，否则呈现两段。
_Avoid_：目录名（指物理目录时除外）、标题

**汇聚边（Aggregate Edge）**：
折叠态下落在组节点上的关系边，代表组内文档与对端之间按 `spec-00001-FR-28` 口径合并后的若干条声明；展开即拆回真节点。
_Avoid_：聚合边、折叠边、组边

**顶层文档（Top-level Document）**：
路径不足三段的文档——直接位于类型目录下，或直接位于 `docs/` 下——不属于任何目录组。
_Avoid_：根文档、散文件

## Example Dialogue

> **Dev**：用户在 prd 节点上点了「接收」，我要把它标成 resolved 吗？
> **Domain expert**：不。prd 是 living doc，接收把它促为 `active`；`resolved` 只属于 work item。这次接收要留痕——一次 commit，写明是对哪个 id 的接收。
> **Dev**：那用户觉得 prd 还没写清楚呢？
> **Domain expert**：那是澄清，不是拒绝——我们没有拒绝这个动作。点澄清，白板拉起一个 agent 会话，带着这份 prd 和它的关系文档逐题问用户，一次一题、推荐项排在首位；确认的未决点进它的 Open Questions，文档保持 `draft`。之后用户可以再推进：点加号，流程配置说 prd 的下一步是 spec，白板就发起一个 agent 会话去写，会话只能动 `docs/`。
