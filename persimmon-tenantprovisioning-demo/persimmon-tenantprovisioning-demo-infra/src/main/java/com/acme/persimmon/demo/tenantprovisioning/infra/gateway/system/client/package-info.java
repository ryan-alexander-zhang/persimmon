/**
 * External system clients for {@code system}.
 *
 * <p>Clients encapsulate protocol-level communication with an external system (HTTP, RPC, SDK).
 * Keep retries, timeouts, authentication, and serialization details here.
 */
package com.acme.persimmon.demo.tenantprovisioning.infra.gateway.system.client;
