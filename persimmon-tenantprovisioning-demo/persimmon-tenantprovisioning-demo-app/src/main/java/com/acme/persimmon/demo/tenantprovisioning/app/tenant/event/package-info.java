/**
 * Application-level events for the {@code biz} context.
 *
 * <p>These packages host event publishing/handling concerns that belong to orchestration rather
 * than pure domain modeling. Typical examples are bridging domain events into integration events or
 * coordinating transactional event publishing.
 */
package com.acme.persimmon.demo.tenantprovisioning.app.tenant.event;
