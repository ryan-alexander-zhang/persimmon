/**
 * MQ message DTOs for the {@code biz} context (optional).
 *
 * <p>Defines the message payload shapes (incoming) used by consumers. Keep them versioned and
 * backward compatible when messages are shared across services.
 *
 * <p><strong>Optional:</strong> use when the inbound message schema is not identical to application
 * DTOs and must be maintained independently.
 */
package com.acme.persimmon.demo.tenantprovisioning.adapter.mq.biz.message;
