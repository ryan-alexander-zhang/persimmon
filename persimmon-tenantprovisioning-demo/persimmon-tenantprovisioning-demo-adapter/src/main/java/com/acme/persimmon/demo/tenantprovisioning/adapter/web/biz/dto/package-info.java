/**
 * Web-layer DTOs for the {@code biz} context.
 *
 * <p>These DTOs represent the HTTP contract (request/response) for this business context. Keep them
 * decoupled from domain types and avoid reusing application DTOs directly if the web contract
 * differs.
 */
package com.acme.persimmon.demo.tenantprovisioning.adapter.web.biz.dto;
