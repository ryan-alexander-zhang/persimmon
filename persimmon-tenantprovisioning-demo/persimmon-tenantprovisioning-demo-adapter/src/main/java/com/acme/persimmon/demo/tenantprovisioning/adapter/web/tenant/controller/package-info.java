/**
 * HTTP controllers for the {@code biz} context.
 *
 * <p>Controllers expose endpoints, validate requests, map them into application commands/queries,
 * and translate application results into HTTP responses.
 *
 * <h2>Usage</h2>
 *
 * <ul>
 *   <li>Keep controllers thin: no business rules.
 *   <li>Prefer delegating to command handlers / query services in the app module.
 * </ul>
 */
package com.acme.persimmon.demo.tenantprovisioning.adapter.web.tenant.controller;
