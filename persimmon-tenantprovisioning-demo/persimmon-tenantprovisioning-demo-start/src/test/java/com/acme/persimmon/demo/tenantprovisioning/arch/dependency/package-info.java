/**
 * Package/class dependency rules.
 *
 * <p>Rules in this package focus on the dependency graph between packages and classes. They are
 * used to prevent boundary erosion (e.g. domain depending on infrastructure) and to detect cycles
 * early.
 */
package com.acme.persimmon.demo.tenantprovisioning.arch.dependency;
