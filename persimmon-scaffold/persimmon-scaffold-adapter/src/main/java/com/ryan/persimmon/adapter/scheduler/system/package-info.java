/**
 * System-level scheduler adapters (cross-cutting).
 *
 * <p>This package hosts scheduled jobs that are not bounded to a specific business context (BC),
 * such as infrastructure support tasks (e.g., outbox relay).
 */
package com.ryan.persimmon.adapter.scheduler.system;

