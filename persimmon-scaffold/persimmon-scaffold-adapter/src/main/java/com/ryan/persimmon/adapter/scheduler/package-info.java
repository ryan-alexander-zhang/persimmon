/**
 * Scheduler inbound adapter (optional).
 *
 * <p>Contains scheduled jobs (cron/trigger-based) that invoke application use cases. This adapter
 * handles scheduling concerns and should avoid embedding business logic.
 *
 * <p><strong>Optional:</strong> use when periodic/background tasks are needed for the system.
 */
package com.ryan.persimmon.adapter.scheduler;
