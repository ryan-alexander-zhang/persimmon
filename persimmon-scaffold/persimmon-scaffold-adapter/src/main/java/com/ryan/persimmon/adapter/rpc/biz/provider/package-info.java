/**
 * RPC providers for the {@code biz} context (optional).
 *
 * <p>Providers implement RPC service interfaces and act as inbound adapters. They should validate
 * incoming requests, call application use cases, and map results to RPC responses.
 *
 * <p><strong>Optional:</strong> use when exposing the {@code biz} context through RPC frameworks.
 */
package com.ryan.persimmon.adapter.rpc.biz.provider;
