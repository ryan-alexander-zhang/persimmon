/**
 * Read-side DTOs for the {@code biz} context (optional).
 *
 * <p>DTOs represent query results and intermediate projections used by query
 * mappers/implementations. Keep them infrastructure-owned and map to application query DTOs as
 * needed.
 *
 * <p><strong>Optional:</strong> use when query result shapes are tailored to storage schemas.
 */
package com.ryan.persimmon.infra.query.biz.dto;
