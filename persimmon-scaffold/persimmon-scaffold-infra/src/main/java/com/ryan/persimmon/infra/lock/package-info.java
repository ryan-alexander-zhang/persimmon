/**
 * Locking and concurrency infrastructure.
 *
 * <p>Contains technical locking mechanisms (distributed locks, local locks) used to protect
 * critical sections. Keep locking implementation details here and expose minimal abstractions to
 * higher layers.
 */
package com.ryan.persimmon.infra.lock;
