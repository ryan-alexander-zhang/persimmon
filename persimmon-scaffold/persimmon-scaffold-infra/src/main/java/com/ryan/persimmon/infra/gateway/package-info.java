/**
 * External system gateway implementations.
 *
 * <p>Contains implementations of domain gateway ports grouped by external system (system-first).
 * This keeps integration code organized by the remote system rather than by business context.
 */
package com.ryan.persimmon.infra.gateway;
