/**
 * Command (write-side) use cases for the {@code biz} context.
 *
 * <p>Commands represent intent to change state. They typically validate input, invoke domain
 * behavior, and persist the resulting aggregate changes through domain repository ports.
 */
package com.ryan.persimmon.app.biz.command;
