/**
 * Query (read-side) use cases for the {@code biz} context.
 *
 * <p>Queries retrieve information without changing domain state. Query use cases may read from
 * optimized read models, projections, or direct query stores depending on architecture choices.</p>
 */
package com.ryan.persimmon.app.biz.query;

