/**
 * Component scan boundaries.
 *
 * <p>Defines component scan configuration to ensure clean module boundaries and prevent accidental
 * wiring across unrelated packages. Use this to control what gets discovered by the DI container.
 */
package com.ryan.persimmon.start.config.scan;
