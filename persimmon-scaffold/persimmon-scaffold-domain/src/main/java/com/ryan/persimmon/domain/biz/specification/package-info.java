/**
 * Specifications for the {@code biz} context.
 *
 * <p>Specifications encapsulate predicates that can be reused and composed (AND/OR/NOT) to express
 * business constraints such as filtering, validation, and eligibility checks.</p>
 *
 * <h2>Usage</h2>
 * <ul>
 *   <li>Prefer specifications when rules must be combined and reused.</li>
 *   <li>Keep them side-effect free and deterministic.</li>
 * </ul>
 */
package com.ryan.persimmon.domain.biz.specification;

