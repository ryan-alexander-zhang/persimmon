/**
 * Specifications for the {@code biz} context.
 *
 * <p>Specifications encapsulate predicates that can be reused and composed (AND/OR/NOT) to express
 * business constraints such as filtering, validation, and eligibility checks.
 *
 * <h2>Usage</h2>
 *
 * <ul>
 *   <li>Prefer specifications when rules must be combined and reused.
 *   <li>Keep them side-effect free and deterministic.
 * </ul>
 */
package com.ryan.persimmon.domain.biz.specification;
